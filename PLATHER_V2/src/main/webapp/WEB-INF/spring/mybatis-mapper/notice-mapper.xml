<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.jhm.plather.dao.NoticeDao">
	<select id="selectAll" resultType="NoticeDTO">
		SELECT * FROM tbl_notice
	</select>
	<select id="findByMaxNcode" resultType="String">
		SELECT MAX(n_code) FROM tbl_notice
	</select>
	<select id="findById" resultType="NoticeDTO">
		SELECT * FROM tbl_notice WHERE n_code = #{n_code}
	</select>
	<insert id="insert" parameterType="NoticeVO">
		INSERT INTO tbl_notice(n_code, n_title, n_content, n_date, n_time, n_hit)
		VALUES(#{n_code}, #{n_title}, #{n_content}, CURRENT_DATE(), CURRENT_TIME(), #{n_hit})
	</insert>
	<update id="update" parameterType="NoticeVO">
		UPDATE tbl_notice SET
		n_title = #{n_title},
		n_content = #{n_content},
		n_date = CURRENT_DATE(),
		n_time = CURRENT_TIME()
		WHERE n_code = #{n_code}
	</update>
	<delete id="delete">
		DELETE FROM tbl_notice WHERE n_code = #{n_code}
	</delete>
</mapper>